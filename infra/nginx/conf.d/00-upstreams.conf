# WebSocket / HMR : Connection dépend de l'en-tête Upgrade du client.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Les cibles sont résolues à la demande dans les server blocks. Les noms
# `web` et `cms` sont fournis par le DNS interne de Docker.
